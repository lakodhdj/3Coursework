<Page x:Class="Coursework.Pages.CategoriesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="clr-namespace:Coursework.Converters"
      Title="Categories">
    <Page.Resources>
        <local:ImagePathConverter x:Key="ImagePathConverter"/>
    </Page.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0 0 0 10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="250"/>
                    <ColumnDefinition Width="350"/>
                    <ColumnDefinition Width="165"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Введите название" FontSize="14"/>
                    <TextBox x:Name="SearchProductName" Width="200" TextChanged="SearchProductName_TextChanged"/>
                </StackPanel>

                <StackPanel Grid.Column="1">
                    <TextBlock Text="Сортировать" FontSize="14"/>
                    <ComboBox x:Name="SortProductCategory" Width="300" SelectionChanged="SortProductCategory_SelectionChanged">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Display}"/>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                </StackPanel>

                <Button x:Name="CleanFilter" Grid.Column="2" Content="Очистить фильтр" FontSize="12" 
                        Click="CleanFilter_OnClick" Margin="10 20 0 0" Style="{StaticResource CustomButtonStyle}"/>
            </Grid>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Margin="0 50 0 10" Grid.Row="0" VerticalAlignment="Bottom">
            <StackPanel Orientation="Vertical" Margin="0 5 10 0">
                <TextBlock Text="Название категории" Foreground="Gray"/>
                <TextBox x:Name="CategoryNameTextBox" Width="200"/>
            </StackPanel>
            <StackPanel Orientation="Vertical" Margin="0 5 10 0">
                <TextBlock Text="Описание" Foreground="Gray"/>
                <TextBox x:Name="DescriptionTextBox" Width="200"/>
            </StackPanel>
            <StackPanel Orientation="Vertical" Margin="0 5 10 0">
                <TextBlock Text="Путь к фото" Foreground="Gray"/>
                <TextBox x:Name="ImagePathTextBox" Width="200"/>
                <Button Content="Выбрать" Click="ChooseImage_Click" Style="{StaticResource CustomButtonStyle}"/>
            </StackPanel>
            <Button Content="Добавить" Click="AddCategory_Click" Style="{StaticResource CustomButtonStyle}" Height="55"/>
        </StackPanel>

        <ListView x:Name="CategoriesListView" Grid.Row="1" SelectionChanged="CategoriesListView_SelectionChanged">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Название" Width="150" DisplayMemberBinding="{Binding CategoryName}"/>
                    <GridViewColumn Header="Описание" Width="200" DisplayMemberBinding="{Binding Description}"/>
                    <GridViewColumn Header="Фото" Width="150">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Image Width="100" Height="100" Source="{Binding ProductImage, Converter={StaticResource ImagePathConverter}}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>

        <StackPanel Orientation="Horizontal" Grid.Row="2" Margin="5 0">
            <Button Content="Сохранить изменения" Click="SaveCategoryChanges_Click" Margin="5 0" Style="{StaticResource CustomButtonStyle}"/>
            <Button Content="Удалить" Click="DeleteCategory_Click" Margin="5 0" Style="{StaticResource CustomButtonStyle}"/>
            <Button Content="Назад" Click="BackToMenu_Click" Margin="5 0" Style="{StaticResource CustomButtonStyle}"/>
        </StackPanel>
    </Grid>
</Page>